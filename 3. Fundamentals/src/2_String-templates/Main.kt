package `2_String-templates`

fun main() {
  // Strings
  val myStr = "Hello World"
  var firCharInStr = myStr[0]
  var lastCharInStr = myStr[myStr.length - 1]

  print("First character $firCharInStr and the length of myStr is ${myStr.length}")
}